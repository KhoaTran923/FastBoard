packages:
  - 'client'
  - 'server'
  - 'wasm'

# pnpm blocks lifecycle scripts by default; esbuild needs its install script
# to fetch the platform binary used by Vite, tsx, and Vitest.
allowBuilds:
  esbuild: true
