packages:
  - 'client'
  - 'server'
  - 'wasm'

# Allow these dependencies to run their install/build scripts (pnpm v10+
# blocks lifecycle scripts by default). esbuild needs it to fetch its
# platform-specific binary used by Vite, tsx, and Vitest.
allowBuilds:
  esbuild: true
